## What

Describe what this pull request adds, removes, or changes.

## Why

Explain the user or maintainer problem this change addresses.

## Testing

List the commands and manual checks that passed. Every change must pass
`scripts/check.sh --offline`; release candidates must also pass the full
`scripts/check.sh` gate in a protected maintainer environment.

## Security and privacy

Describe any effect on credentials, prompts, request routing, telemetry,
localhost APIs, dependencies, release artifacts, or network destinations.
Write "None" when the change has no effect.
