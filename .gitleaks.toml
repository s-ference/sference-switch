[extend]
useDefault = true

[[allowlists]]
description = "Synthetic token used only by the isolated fresh-install stub"
regexTarget = "match"
regexes = [
  '''sference-switch-fresh-install-local-token''',
]
